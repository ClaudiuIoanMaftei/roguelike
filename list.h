///List Functions

#include "dependencies.h"
/*
struct point
{
	int x;
	int y;
	int number;
	point *prev=NULL;
	point *next=NULL;
}

struct pointList
{
	point *first=NULL;
	point *last=NULL;
	int length=0;

	void pointListAdd(int x,int y)
    {
        point *target;

        target->x=x;
        target->y=y;
        target->number=length;
        target->prev=last;
        target->next=NULL;

        last->next=target;
        last=target;
        length++;
    }
    */
}
